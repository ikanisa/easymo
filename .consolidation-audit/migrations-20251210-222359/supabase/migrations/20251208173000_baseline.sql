-- Baseline migration - current remote schema is already applied
-- This migration intentionally does nothing
-- All schema changes before this point are considered applied
SELECT 'Baseline established - remote schema is truth' as status;
