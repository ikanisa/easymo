/**
 * Placeholder export for the typed client SDKs.
 * Legacy callers in `apps/api/` and `services/*` should continue to use their
 * existing helpers until we begin moving code into this package.
 */
export const clientsPlaceholder = {
  message: "@easymo/clients is not implemented yet."
};
