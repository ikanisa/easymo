export {
  getSipWebhookControllerBasePath,
  getSipWebhookEndpointMethod,
  getSipWebhookEndpointPath,
  getSipWebhookEndpointSegment,
  sipWebhookRouteDefinitions,
}
  from "@easymo/commons";

export type { SipWebhookControllerKey, SipWebhookEndpointKey, SipWebhookRoutes } from "@easymo/commons";
