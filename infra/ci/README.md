# CI Infrastructure (Strangler Fig)

New GitHub Actions and Vercel orchestration will grow here alongside the current scripts found in [`../../infrastructure`](../../infrastructure) and [`../docker`](../docker).

As workflows are modernised we will mirror critical jobs in this directory to ensure safe cutovers before deleting the original automation.
