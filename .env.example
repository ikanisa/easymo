# easyMO environment variable examples (no secret values)
# Copy to .env.local for local development. Never commit populated secrets.

# Supabase project configuration
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""      # Server-side only, never expose to client bundles
SUPABASE_JWT_SECRET=""             # Must match Supabase project's JWT secret
SUPABASE_ACCESS_TOKEN=""          # Used by Supabase CLI for migrations/links
SUPABASE_DB_PASSWORD=""           # Optional: database password for CI migrations

# Application URLs
NEXT_PUBLIC_APP_URL=""             # Canonical URL for production (e.g., https://admin.easymo.example)
NEXT_PUBLIC_PREVIEW_APP_URL=""     # Preview URL placeholder (Vercel auto-generated)

# Deployment automation
VERCEL_TOKEN=""                   # Personal token for CI-driven Vercel deployments
GITHUB_TOKEN=""                   # Fine-grained token for GitHub API automation (branch protections)

# Monitoring / analytics (optional)
NEXT_PUBLIC_POSTHOG_KEY=""         # Example analytics key if instrumented
NEXT_PUBLIC_SENTRY_DSN=""          # Example Sentry DSN for browser error tracking
