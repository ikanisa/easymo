# Environment variables for the EasyMO admin panel and Supabase functions

# URL of your Supabase project (https://xxxx.supabase.co)
VITE_SUPABASE_URL=

# Supabase public anon key (used by client‑side code).  Do not expose the
# service role key on the client.
VITE_SUPABASE_ANON_KEY=

# Supabase service role key – used on the server and inside Supabase edge
# functions.  For the admin panel (trusted builds only) expose it via a
# `VITE_` prefixed variable so Vite can inject it at build time.  Never ship
# this key to untrusted clients.
VITE_SUPABASE_SERVICE_ROLE_KEY=

# Secret token used to authenticate admin API requests from the front‑end
# to the Supabase edge functions.  Set the same value in your Vercel and
# Supabase function secrets.
EASYMO_ADMIN_TOKEN=

# Allowed origins for CORS (comma separated).  Used by Supabase edge
# functions to restrict external calls if needed.
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Toggle to indicate production environment.  Used by error handling and
# logging utilities.
IN_PRODUCTION=false

# RSA keys for signing/verification of WhatsApp messages or tokens.  If
# you intend to implement message signing provide your own key pair here.
RSA_PRIVATE_KEY=
RSA_PUBLIC_KEY=